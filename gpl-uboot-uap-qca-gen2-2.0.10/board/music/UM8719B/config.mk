ifdef BOOT_FROM_NAND
	TEXT_BASE = 0x80000000
else

ifdef BOOT_FROM_SPI_RAM
	TEXT_BASE = 0x81000000

ifdef ASDK_SPI_BOOTROM_MODE
	PLATFORM_CPPFLAGS += -DASDK_SPI_BOOTROM_MODE
endif

else #BOOT_FROM_SPI_RAM

ifdef ASDK_SPI_BOOTROM_MODE
	PLATFORM_CPPFLAGS += -DASDK_SPI_BOOTROM_MODE
	TEXT_BASE = 0xb9000000
else
	TEXT_BASE = 0xbf000000
endif

endif #BOOT_FROM_SPI_RAM endif

endif #BOOT_FROM_NAND endif




# ROM version
#TEXT_BASE = 0xbf000000

# SDRAM version
#TEXT_BASE = 0x80000000
